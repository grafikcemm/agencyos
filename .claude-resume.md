# Resume Brief — agency-os-v2-sprint0 — 2026-07-13 (Faz A-D kısmi kapanış)

## 2026-07-13 EK-3 (HEAD d3f4b96): FAZ 0 TAMAM — 0.1 doküman-gerçeklik, 0.2 atomik lead-action RPC (mig 058 HAZIR-onay bekliyor; kod RPC-first+fallback), 0.3 claim durum makinesi (mig 006-LIFE HAZIR-onay bekliyor; kod complete/fail+prod fail-closed 503), 0.4 pending durable-truth, 0.5 dispatch at-most-once claim makinesi, 0.6 setup POST-only+health+script APP_URL-preflight. Faz 1 mini (telefon filtresi, süre=zorunlu-5, note≠done, later tarih girişli, refresh) + 2.1 wa.me lint kapısı + route envanteri (docs/route-inventory-2026-07-13.md). Kapılar: vitest 779 · Playwright 37×2 · lint 0/0 · build ✓. SIRADAKİ ONAYLAR: mig 058 (App+test birlikte) + mig 006 (LIFE). KALAN: Faz 1.3 inline-contact/recipient-digest, 1.4 batch classify+dedupe+p95, 2.2-2.5 evidence-map/VoiceDNA-UI/rol-prompt-genişletme/follow-up motoru, Faz 3 fake-transport valid-inbound E2E + izole LIFE test şeması, Faz 4 OAuth/reply/teklif/analitik. Coverage hedef-altı: today %18, salesHandlers %0, sequences %0, coldEmail %36 (rapor edildi).

## 2026-07-13 EK-2 (HEAD e8f10c2+): mig **005-LIFE + 045 + 057 ÜÇÜ DE CANLI** (kullanıcı onayı; migration history doğrulanabilir). Test DB paritesi 62 tablo byte-identical; audit E2E kanıtlı. TELEGRAM_USER_ID Vercel'de MEVCUT (kullanıcı beyanı). Kalan kullanıcı adımları: push → Vercel Promote → `node scripts/telegram-register-webhook.mjs` (canlıda webhook URL BOŞ — inbound ölü). DURUM ETİKETLERİ: 'kod hazır+unit' = Faz B/C/D modülleri; 'E2E kanıtlı' = kokpit/HITL/telegram-auth; 'canlı çalışıyor' = YALNIZ migration'lar + cron outbound; 'canlı DEĞİL' = webhook inbound, yeni kod (deploy bekliyor).

## Current goal
Üretim-hazırlık + gelir-operasyonu sprinti (Faz A-F). **Faz A (baseline) + Faz B (Telegram P0) + Faz C (/bugun 1.2-1.5) TAM; Faz D kısmi (mig 045 hazır + kalite lint canlı-advisory); Faz E/F NOT RUN.**

## Done so far (bu worktree, branch feat/agencyos-v2-sprint0)
- **Faz A:** baseline kapılar (tsc/lint 0-0/vitest 688/build/E2E 28×2) + coverage dökümü (coldEmail-orchestrator-jarvis-memory %0; today %20) + route envanteri (73 route, ~10 E2E-driven).
- **Faz B (Telegram P0):**
  - `src/lib/telegram/client.ts` — tek transport: timeout+AbortController, yalnız-429 in-process retry, typed Result (message_id'siz başarı YOK), token redaction; sendMessage/getMe/getWebhookInfo/setWebhook (setWebhook HİÇBİR YERDEN otomatik çağrılmaz).
  - `reminderDispatch.ts` — orchestrator/weekly-retro sahte başarı düzeltmesi: başarısız send → conversation log YOK + `send_failed`+attempts+next_retry_at; `filterSentToday` retry kilidini açar; başarılı retry aynı satırı günceller (tek kayıt).
  - Webhook (`api/telegram/route.ts`): Zod şema + 64KB sınır + bozuk JSON 400; **fail-closed auth: secret + chat + TELEGRAM_USER_ID (yeni env, ZORUNLU)** — hepsi her DB yazımından ÖNCE; durable update-claim (`telegram_update_claims`, tablo yoksa memory fallback mode='memory'); **satış komutları taahhüt yakalayıcısından ÖNCE**; "görev ekle"→1/2 TTL'li pending state; bare "onayla/gönder" pending action yoksa MUTASYONSUZ.
  - `salesCommands/salesHandlers` — /bugun /aranacaklar /taslaklar /takipler /sorunlar /pipeline + satır aksiyonları + "X için cold email hazırla" (GERÇEK draft kaydı, 'gönderilebilir' izlenimi yok) — hepsi getTodayCockpit + applyLeadAction'dan (web ile AYNI kimlik/sıra).
  - `api/telegram/diagnostics` + settings'te read-only panel (secret göstermez).
- **Faz C:** `leadActions.ts` (geçiş kuralları+CAS+idempotency+audit; tablo yoksa 'degraded'); `/api/leads/[id]/action`; CallListPanel (5 mutlaka-bugün + backlog + süre/ilerleme + sıradaki-en-iyi + aksiyon butonları); telefon-dedupe (`normalizePhoneKey`, review notu, otomatik merge YOK); loadPendingSends TÜM taslakları 9-durumla döner (`classifyDraftState`, shared.ts) + tek güvenli next action; SendIssuesPanel Reconcile butonu; sidebar İcraat-Fırsatları kaldırıldı; /dashboard→/bugun redirect.
- **Faz D (kısmi):** `qualityLint.ts` deterministik kalite lint'i (subject/bağlam/cliché/spam/tek-CTA/kanıtsız-iddia/opt-out/VoiceDNA-yasak-ifade) + 10 golden-set testi; request-send response'una ADVISORY quality alanı; mig 045 contacts HAZIR (uygulanmadı).
- **Migration'lar (HAZIR, UYGULANMADI — onay şart):** LIFE `supabase/life-migrations/005_telegram_reliability.sql` (+rollback; update claims + pending actions), App `migrations/057_lead_action_audit.sql` (+rollback), App `migrations/045_contacts.sql` (+rollback). Kod üçü olmadan da çalışır (degraded/memory mod görünür).

## Tests
tsc ✓ · lint 0/0 ✓ · vitest 755/755 ✓ · build ✓ · Playwright 37/37 (telegram-auth 8 + bugun satır-aksiyon/dedupe/darboğaz/reconcile eklendi) — son koşu kanıtı commit mesajında.

## Open problems / KALAN İŞ
- Faz D tam: contacts CRUD + rol-aware üretim girdisi + Voice DNA original/final delta öğrenme döngüsü + lint'i BLOCKING yapma (kalibrasyon sonrası).
- Faz E: Gmail OAuth+Vault (doc 32; dış konfig + bağımsız güvenlik incelemesi ŞART; GMAIL_SEND_ENABLED=false) + reply ingest + follow-up FSM.
- Faz F: teklif motoru (mig 048/049) + gelir analitiği.
- Deploy env EKSİĞİ: `TELEGRAM_USER_ID` Vercel'e girilmeli (yoksa webhook fail-closed hiçbir mesajı işlemez!) + webhook secret'lı yeniden kayıt (kullanıcı onayıyla, diagnostics panelinden durum görülür).
- Telegram gerçek inbound: webhook URL kaydı doğrulanmadı (E2E yalnız auth-reddi yollarını sürer) → GERÇEK inbound NOT RUN.
- Jarvis engine'in generate_pitch yolları hâlâ salt metin (yalnız telegram prepare_draft gerçek kayıt üretiyor).

## Commands to resume
```bash
cd c:/Users/alice/.gemini/antigravity/scratch/agency-os-v2-sprint0  # cwd ana repoya SAPABİLİR — hep explicit cd
git log --oneline -3
npm test && npm run test:e2e
```
