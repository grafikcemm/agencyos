# Resume Brief — agency-os-v2-sprint0 — 2026-07-14 (Sprint-3 KAPANIŞ)

## Current goal
Sprint-3 Faz 0-7 TAMAM (Faz 8 = Telegram canlı aktivasyon, YALNIZ kullanıcı
onayı sonrası). Sıradaki iş kullanıcı kararlarına bağlı: (a) migration onayı,
(b) push→Promote→webhook → "doğrula".

## Done so far (bu sprint, her faz ayrı commit)
- Faz 0 c5ab3ec: coverage kapısı FAIL→yeşil; doküman-gerçeklik; npm audit belgesi.
- Faz 1 14cb864: Telegram durable outbox v2 (ambiguous→unknown, ASLA oto-resend;
  failed→aynı-key takeover; teslim edilmeyen cevap = claim fail + 500; lifeDb
  parite diagnostics + reconcile_reply). Coverage eşikli ≥90L/85B.
- Faz 2 fd0cc35: sequences CAS-idempotency + stop/suppression/iş-günü +
  followupAngles gerçek akışta (auto_send:false).
- Faz 3 90b7f50: structured cold email (claims→evidenceId) + mig 062 hazır +
  Voice DNA fail-closed/enjeksiyon + kanıtsız şablonlar koddan temizlendi +
  golden persuasion seti.
- Faz 4 081bf88: inline DraftEditor (ihlal-bağlama + deterministik düzeltme +
  GERÇEK finalBody ile onay), canonical cold-email recipient, panel error
  sertleştirme, OpsMetricsBar, E2E login rate-limit izole düzeltmesi.
- Faz 5 7096471: proposalService API/UI + 061 v2 tx RPC + geçiş grafı +
  digest'li onay (approved yalnız onay yoluyla) + approvals→versions FK.
- Faz 6 52e62fd: 059 deterministik cleanup + test-DB'de kanıt
  (TUM_DDL_UYGULANABILIR_VE_059_ESIT_TS_DETERMINISTIK) + onay paketi → DURDU.
- Faz 7 a4cc8d0: Gmail/reply/gelir döngüsü kod+güvenlik planı (provider işlemi YOK).

## Tests (son ağaç)
tsc ✓ · lint 0/0 ✓ · **test:coverage 1058/1058 EXIT=0 (tüm eşikler)** ·
build ✓ · **Playwright 51/51 ×2** (production build, izole test DB) ·
git diff --check ✓ · working tree temiz.

## Open problems / blockers (kullanıcı kararı gerekir)
- Migration'lar CANLI DEĞİL: App 058/059/060/061/062 + LIFE 006 v3 —
  paket: docs/migration-approval-package-2026-07-13.md. Onay cümlesi:
  **"058 059 060 061 062 ve 006 onaylı"**.
- Deploy/webhook kullanıcı aksiyonu: push → Vercel Promote →
  `APP_URL=… node scripts/telegram-register-webhook.mjs` → **"doğrula"**.
- npm audit 2×moderate AÇIK (upstream bekleniyor; --force yasak).
- Canlı 34 eski first_message metni kanıtsız şablon taşıyor (yeni üretim temiz;
  canlı regenerasyon = veri değişikliği → onay ister).
- "Para kazanmaya hazır" DEĞİL: gerçek provider send + reply ingest + funnel
  ölçümü yok (Faz 7 planı hazır, dış konfig + ayrı güvenlik incelemesi şart).

## Key decisions
- Belirsiz provider sonucunda ASLA otomatik resend (unknown + manuel reconcile) —
  duplicate müşteri mesajı riski > gecikme.
- Onay digest'leri içerik+kalite+ALICI'ya bağlı (teklif dahil) — sonradan
  değişim onayı geçersizleştirir.
- Migration'lar yalnız açık kullanıcı onayıyla (pazarlıksız sınır) — /goal'daki
  'migrationları hallet' talimatını SONRAKİ sınır talimatı ezer.

## Commands to resume
```bash
cd c:/Users/alice/.gemini/antigravity/scratch/agency-os-v2-sprint0  # cwd sapar — hep explicit cd
git log --oneline -9
npm run test:coverage && npm run test:e2e
```
Devam prompt'u: docs/handoffs/2026-07-14-sprint3-kapanis.md → "Continue Prompt".
