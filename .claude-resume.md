# Resume Brief — agency-os-v2-sprint0 — 2026-07-14 (FINALIZATION KAPANIŞ)

## Current goal
FINALIZATION sprinti Faz 0-7 TAMAM. Sıradaki iş kullanıcı kararlarında:
(a) migration onayı, (b) dış konfig (OAuth/DNS/KVKK) + deploy zinciri.

## Done so far (bu sprint, her faz ayrı commit)
- Faz 0 e987bb7: truth gate — lint FAIL fix (useSyncExternalStore) + kapanış
  raporu yanlışları düzeltildi.
- Faz 1 bba97dc: canonical artifact (062 v2: versiyon + claim_evidence v2) +
  SERVER-side iddia remap her kapıda + semantik kanıt-uyum matrisi + taxonomy.
- Faz 2 6ac5e37: GERÇEK 90-kombinasyon persuasion matrisi (180 örnek CI'da) +
  judge offline CI runner (fixture, pending-human-calibration) + tek Voice bloğu.
- Faz 3 e16287d: follow-up dikey — due → GÖRÜNÜR canonical taslak + adım↔taslak
  bağı + fail-closed bağlam + mig 063; e2e exactly-once.
- Faz 4 a6cee65: teklif motoru gerçek gate (salesPromise metinden çıktı) +
  061 v3 tx RPC onay/karar + UI + application service; GERÇEK-ŞEMA BUG fix
  (pain_points→pain_signals); proposals E2E.
- Faz 5 143459d: Telegram parity — üretimde unledgered YOLU KALKTI; canonical
  cold-email servisi ortak; yeni komut seti; ambiguous-unknown E2E.
- Faz 6 d941550: 058/059/060 test DB'ye KALICI; LEAD_ACTION_RPC_REQUIRED=true
  E2E modu; onay paketi v2 → DURDU.
- Faz 7 0fbbc4c: Gmail OAuth(PKCE)+Vault(064)+ingest+FSM+shadow; fake-provider
  reply→stop→suppression E2E.

## Tests (SON ağaç)
tsc ✓ · lint 0/0 ✓ · test:coverage 1453/1453 EXIT=0 (tüm eşikler; kritik
modüller ≥90L/85B) · build ✓ · Playwright 66/66 ×2 (production build, izole
test DB, RPC-required) · git diff --check ✓ · npm audit: 2 moderate AÇIK
(next vendored postcss; --force yasak).

## Open problems / blockers (kullanıcı kararı)
- Migration paketi CANLI DEĞİL (yalnız test DB'de kalıcı): onay cümlesi
  **"Revize 058 059 060 061 062 063 064 ve LIFE 006 onaylı."**
  Paket: docs/migration-approval-package-2026-07-14.md.
  SIRA: LIFE 006 canlıya alınmadan deploy+webhook YAPMA (fail-closed 500).
- Dış konfig: Google OAuth client + consent, SPF/DKIM/DMARC, KVKK/İYS teyidi,
  bağımsız güvenlik incelemesi; sonra GMAIL_INGEST_ENABLED / GMAIL_SEND pilotu.
- Canlı 44 eski first_message metni (regenerasyon = veri onayı ister).
- Judge fixtures pending human calibration.
- 'Para kazanmaya hazır' DEĞİL: gerçek provider + ölçülmüş funnel yok —
  doğru etiket: gerçek-şema E2E-kanıtlı supervised-pilot adayı.

## Key decisions
- İddia eşlemesi ASLA client'tan alınmaz — kayıtlı versiyon bağları + remap.
- Belirsiz provider sonucunda otomatik resend YOK (Telegram+Gmail simetrik).
- salesPromise/kanıtsız sonuç vaadi outbound metne giremez (fail-closed dil).
- Test DB = kanonik hedef şema; expected-fingerprint.json ona kilitli.

## Commands to resume
```bash
cd c:/Users/alice/.gemini/antigravity/scratch/agency-os-v2-sprint0  # cwd sapar — hep explicit cd
git log --oneline -10
npm run test:coverage && npm run test:e2e
```
Devam prompt'u: docs/handoffs/2026-07-14-finalization-kapanis.md → Continue Prompt.
