# AgencyOS — Güncel Devam Notu (2026-08-10, ikinci tur)

## Codex kapanış eki (2026-08-10, üçüncü tur)

Önceki nottaki yerel ve dış kapıların büyük bölümü kapandı:

- Command Center; bugünün kararları, kampanya sağlığı ve TR/Global funnel tek
  ekranda olacak şekilde yeniden kompoze edildi. Teknik telemetri katlandı.
- Lead Radar Global; ülke/şehir seçimi, dünya haritası, global Jarvis bağlamı ve
  yönlendirici boş durum aldı. `/firsatlar`, Radar'ın fırsatlar sekmesine kalıcı
  yönleniyor.
- Apify adaptörü `apifyPolicy.ts` ile bağlandı. Actor metadata'sından gerçek
  PAY_PER_EVENT fiyatı okunmadan koşu açılmaz; `maxItems` ve
  `maxTotalChargeUsd` sağlayıcı tarafında da uygulanır. **Hiç Actor koşulmadı,
  kredi harcanmadı ve APIFY_ENABLED açılmadı.**
- Canlı Supabase projesine migration 068–074 uygulandı. 073 yeni lifecycle
  RPC'sinin anon/authenticated EXECUTE açığını kapattı; 074 yinelenen settings
  UNIQUE constraint'ini temizledi. Security advisor: 0 WARN. Performance
  advisor: 0 WARN.
- 50 eski lead silinmeden `legacy-epoch-close` gerekçesiyle emekliye ayrıldı.
  Güncel `epoch-2026-08` içine araştırma kaynağından 60 lead eklendi:
  **45 Türkiye + 15 Global**, pazar/ülke uyumsuzluğu 0.
- Seed mapper ülke/pazar gerçekliğini koruyor; emit-json modu MCP/SQL güvenli
  aktarımı için eklendi. Operasyonel lead okumaları current epoch + active
  kapsamına alındı.
- `127.0.0.1:4318` HMR/harita sorunu `allowedDevOrigins` ile kapandı. Yerel dev
  sunucu 4318'de açık bırakıldı.

Son doğrulama: ESLint 0, TypeScript 0, Vitest **154 dosya / 2.243 test**, Next
production build **91 rota**, masaüstü/mobil gerçek Chromium'da console/page
error 0, yatay taşma 0, Global harita yüklendi, `/firsatlar` yönlendirmesi doğru.

Henüz yapılmayan dış yayın adımları: çalışma ağacını atomik commit etme, remote'a
push, Vercel production deploy ve deploy sonrası smoke. E2E'nin kalan DB akışları
ayrı test Supabase service-role erişimi olmadan koşturulamaz. Grafikcem_OS içine
subtree taşıma AgencyOS yayınından sonraki ayrı fazdır.

## Tek çalışma konumu

- Aktif repo: `C:/Users/alice/.gemini/antigravity/scratch/agency-os`
- Branch: `feat/agencyos-growth-and-life-separation`
- Production: `https://agencyos-zeta-ashen.vercel.app`
- **Hedef konum:** `C:/Users/alice/Grafikcem_OS/agency-os` — taşıma HENÜZ YAPILMADI.

## ⚠️ Deploy gerçeği

Canlı production deployment `source: "cli"`, `meta.actor: "codex"` — **git deployment DEĞİL**.
`git push` ve `git subtree push` deploy TETİKLEMEZ. `.vercel/` gitignore'da, taşımada gelmez.
Vercel CLI kurulu değil. Deploy modeli kullanıcı kararıdır.

Vercel: proje `agencyos` = `prj_d9hDcjEEUZje7esCfJ16ImsVR2ZF`, takım `team_zQznkvwePB8EIW52B6wkcWgb`.

## Ürün sınırı — DEĞİŞTİ

AgencyOS **yalnız** ajansın gelir ve müşteri edinim işletim sistemidir.

**Kariyer AgencyOS'tan ÇIKTI** (2026-08-10 ikinci tur kararı; aynı günün ilk kararını ezer):
`/kariyer` ve `/gelisim` veri okumayan "geçiş hazırlanıyor" ekranı döner, menüdeki `KARİYER`
grubu kaldırıldı, growth snapshot'ından `careerAction` silindi. Kod ve veri **silinmedi**;
devir manifesti `docs/CAREER-HANDOFF-2026-08-10.md`.

Menü: `MÜŞTERİ` (Lead Radar · Pipeline · Deneyler · Projeler · Hizmetlerim · **Belgeler**) ·
`ARAÇLAR` · `SİSTEM`. Kişisel görev/alışkanlık/finans hiçbir koşulda geri dönmez.

Tam tablo: `docs/PRODUCT-BOUNDARY.md`.

## Bu turda eklenen kanonik modüller

| Modül | İş |
|---|---|
| `src/lib/leads/epoch.ts` | edinim dönemi, emeklilik planı, dokunulmaz tablolar |
| `src/lib/leads/marketScope.ts` | Türkiye / Global çalışma alanları, sorgu presetleri |
| `src/lib/compliance/countryPolicy.ts` | ülke rejim kaydı + fail-closed gönderim kararı |
| `src/lib/compliance/leadPolicyGate.ts` | kararın gönderim yoluna bağlanması (approval / send) |
| `src/lib/outreach/outboundCapacity.ts` | 2.500–3.000 kapasite + deliverability hazırlığı |
| `src/lib/growth/apifyPolicy.ts` | $25 kredi bütçesi, fiyat preflight, ölçek eşikleri |
| `src/data/documentCenter.ts` | TR + Global belge merkezi + outreach uyum paketi |
| `src/data/pricingVersions.ts` | versiyonlu fiyat; **onaylı sürüm YOK → teklif kapalı** |
| `scripts/reset-acquisition-epoch.ts` | envanter/export/dry-run/apply/undo/verify |

## Canlı şema durumu

**UYGULANMAMIŞ — kullanıcı onayı bekliyor:**

| Migration | Ne getirir | Ön koşul |
|---|---|---|
| `068_niche_targeting.sql` | niş/kanıt/uyum sütunları, `normalize_domain()`, `domain_normalized` UNIQUE | `npm run seed:research -- --preflight` temiz olmalı |
| `069_lifecycle_actions.sql` | `lead_lifecycle_events`, genişletilmiş `apply_lead_action()` | 068 |
| `070_enrichment_reservations.sql` | atomik sorgu rezervasyonu, `contacts.email_confidence` | 069 |
| `071_acquisition_epoch.sql` | `acquisition_epochs`, `retired_at`, dönem FK'leri | bağımsız |
| `072_market_scope_compliance.sql` | pazar/ülke/uyum olguları, `outreach_campaigns` | bağımsız (sıra: 071 → 072) |

`supabase/life-migrations/008_career_evidence.sql` **AgencyOS'a UYGULANMAZ** — hedef sahiplik
GrafikcemOS (bkz. career handoff).

Hepsinin `_rollback.sql` ikizi var. 071 ve 072 hiçbir satırı silmez.

## Güvenlik sabitleri

- Ülke uyum kapısı **fail-closed**: lead okunamazsa, 072 uygulanmamışsa veya ülke boşsa → `blocked`.
- Suppression mutlaktır: taslak bile üretilmez.
- Tacir/esnaf sayılmayan meslekler (avukat vb.) TR'de bloklu.
- AB/AEA, Kanada, Avustralya ve tanımsız ülkeler bloklu başlar.
- Tahmin edilmiş e-posta (`guessed`) taslağı engellemez, **gönderimi engeller**.
- Gerçek gönderim yolu deliverability hazırlığı (`OUTBOUND_READINESS_CONFIRMED` + ölçümler)
  olmadan açılmaz. Dry-run'da bu kapı aranmaz (hiçbir mesaj dışarı çıkmaz).
- Apify: fiyat preflight geçilmeden koşu açılmaz; sert kesme $22; rezerv $3; pilot ≤200 place / ≤$2.
- Reset aracı hiçbir şey silmez; uyum/denetim tablolarına yazma yolu yoktur.

## `agencyos-ozet` — KAPALI kalır

Koşullar ve çözülmemiş asimetri: `docs/security/cemos-bridge-threat-model.md`.
**Bu turda da bayrak DEĞİŞTİRİLMEDİ.**

## Kalan dış bağımlılıklar

1. Canlı migration (068–072) + seed + epoch reset uygulaması — kullanıcı, SQL Editor/CLI.
2. Apify hesabının gerçek güncel aktör fiyatı; `APIFY_ENABLED` açma kararı.
3. Hizmet kataloğu, taban fiyat, KDV/VAT yaklaşımı — onaylı fiyat sürümü olmadan teklif üretilmez.
4. Tacir/esnaf, İYS/KVKK, CAN-SPAM footer ve ülke bazlı hukuki onaylar.
5. Gönderim domaini/mailbox kurulumu + SPF/DKIM/DMARC + Postmaster ölçümü.
6. Grafikcem_OS'a taşıma + deploy modeli kararı.
7. YOS/Enplus/Dede Oyuncak için yayımlanabilir KPI izni.

## Doğrulama komutları

```powershell
npx tsc --noEmit
npm run lint
npm test
npm run build
npm run test:e2e
npm run seed:research -- --preflight
npm run epoch:reset -- --inventory
```

Not: `next dev` sonrası `.next/dev/types/validator.ts` bayat kalabilir; build öncesi silinir.
Kullanıcıya ait ekran görüntüsü, `.codex/`, `.mcp.json`, `tmp/` ve `reports/` commit edilmez.
