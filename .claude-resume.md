# Resume Brief — agency-os-v2-sprint0 — 2026-07-14 (FINAL PILOT BLOCKERS)

## Current goal
FINAL PILOT BLOCKERS sprinti: 13 audit bulgusunu kapat (gerçek Gmail transport,
atomik OAuth/Vault, history-cursor ingest, cron gerçeği, enrichment,
coldEmailService kapısı, Telegram HITL parity, 30dk kokpit) → Faz 8'de revize
paket test DB'ye + tüm kapılar ×2 → DUR, onay cümlesi iste. Canlı migration/
push/deploy/webhook/gerçek send YOK.

## Done so far
- Faz 0: truth gate — 13 bulgu kod+canlı-DB kanıtıyla DOĞRULANDI
  (docs/final-pilot-blockers-truthgate-2026-07-14.md); baseline tsc+lint PASS.

## Plan (her faz ayrı commit)
1. Gerçek Gmail send + rfc822msgid reconcile + contract testleri
2. OAuth: iki scope ZORUNLU + doğrulanmış e-posta (getProfile/JWKS) +
   replay-safe state + 064 revize (hesaba özgü secret, tek-tx RPC) + Settings UI
3. history.list cursor ingest + sender doğrulama + quarantine + vercel.json
   cron + schedule UI parity (CI testi)
4. Recipient+evidence enrichment (günlük cap, HITL eşiği, kokpite hazır-aksiyon)
5. coldEmailService unit/integration + eşik 90L/85B + fail-closed
6. Telegram signed/TTL pending actions: edit→approve→AYRI send teyidi,
   proposal/reconcile kararları; E2E (stale/replay/suppression)
7. Voice DNA onboarding + outcome telemetry + 30dk bütçeli kokpit + gerçek
   health gate + legacy metin gizleme (regenerasyon YOK — ayrı veri onayı)
8. Revize paket (058-063 + revize 064 + LIFE-mimic 006) test DB'ye sıralı;
   fingerprint + advisors + tüm kapılar ×2 → DUR → yeni onay cümlesi

## Kapı eşikleri (Faz 8)
tsc · lint 0/0 · test:coverage (coldEmailService/Gmail transport/OAuth/Vault/
ingest/Telegram action ≥90L/85B) · build · e2e ×2 · git diff --check ·
npm audit dürüst · git status temiz.

## Key decisions
- unknown@unknown YASAK; hesap e-postası doğrulanmış kaynaktan.
- Vault: hesaba özgü secret adı; store+activate tek transaction RPC.
- Unknown provider sonucu → otomatik resend YOK (değişmedi).
- 44 legacy first_message CANLIDA regenerate EDİLMEZ (ayrı veri onayı);
  UI'da legacy/geçersiz işaretli.
- healthy:true yalnız pilot-ready ise.

## Commands to resume
```bash
cd c:/Users/alice/.gemini/antigravity/scratch/agency-os-v2-sprint0
git log --oneline -12
npm run test:coverage && npm run test:e2e
```
