# AgencyOS V2 — Kapanış / Compact Sonrası Devam (2026-07-15)

## Çalışma konumu

- Worktree: `C:/Users/alice/.gemini/antigravity/scratch/agency-os-v2-sprint0`
- Branch: `feat/agencyos-v2-sprint0`
- Production: `https://agencyos-zeta-ashen.vercel.app`
- Vercel Hobby yeterli. Alt-günlük işler ücretsiz dış scheduler ile çalışıyor; Pro zorunlu değil.

## Tamamlanan teknik kapsam

- App DB migration 058–065 ve LIFE migration 006 canlıda.
- İzole `agencyos-e2e` şeması App fingerprint'iyle eşit. LIFE test yüzeyi (`daily_v2`, `active_tasks`, `telegram_*`) fingerprint dışında ve aynı test projesinde yeniden kurulabilir.
- Google Workspace `grafikcem.com` alan adı doğrulandı; `info@grafikcem.com` aktif.
- MX Google'a geçti. SPF, IHS + Google geçiş uyumlu. DMARC izleme modunda. 2048-bit DKIM doğrulandı.
- Gmail OAuth yalnız `gmail.send` + `gmail.readonly`; token Vault'ta.
- Gmail ingest üretimde açık, history cursor ve scheduler sağlıklı.
- Ticari müşteri gönderimi `GMAIL_SEND_ENABLED=false`; hukuk/uyum tamamlanmadan açılmayacak.
- Gerçek teknik Gmail canary send + gerçek reply + ingest karantina audit'i doğrulandı. Teknik canary müşteri/FSM state'ine dokunmaz.
- İlk canary sırasında Gmail Message-ID eventual-consistency nedeniyle iki teknik ileti çıktı. Kök neden DB unique claim + `claimed|unknown|sent|replied` ledger ile düzeltildi; `unknown` otomatik yeniden gönderilmez. Üretim ledger durumu `replied`.
- Telegram bot webhook'u gerçek üretimde kayıtlı. Gerçek inbound, outbound, durable claim, duplicate/takeover ve imzalı satış aksiyonları doğrulandı.
- `/bugun` en üstünde kullanıcının günlük duası var.
- YouTube cold-outreach incelemesi uygulandı: e-posta-öncelikli yönlendirme, gerçek provider sonuçlarına dayalı niş skor kartı ve kanıtsız iddiaların outbound kapısında fail-closed engellenmesi.
- Teklif, cold email, follow-up ve ikna metinlerindeki kanıtsız sayı/süre/sonuç vaatleri temizlendi. İddialar evidence ID ile bağlanmadan onaya ilerleyemez.
- Next.js 16 deprecated `middleware.ts`, aynı davranış korunarak `src/proxy.ts` convention'ına taşındı.

## Son doğrulama kanıtı

- `npx tsc --noEmit`: PASS
- `npm run lint`: PASS — 0 error / 0 warning
- `npm run test:coverage`: PASS — 128 dosya, 1707/1707 test, config'teki tüm kritik eşikler yeşil
- `npm run build`: PASS — 84 sayfa, deprecated Middleware uyarısı yok
- `npx playwright test --repeat-each=2`: PASS — 150/150; production build + izole test DB; cleanup sıfır
- Gerçek Gmail teknik send + reply: PASS
- Gerçek Telegram inbound + outbound: PASS

## Dürüst ürün kararı

Kod ve provider altyapısı **supervised pilot-ready**. Sistem, kullanıcının kısa sürede günlük müşteri operasyonunu `/bugun` ve Telegram üzerinden yürütme hedefini karşılayacak çekirdek döngülere sahip: lead/contact, puanlama, kanıtlı taslak, düzeltme, HITL onay, takip, reply ingest, opt-out suppression, teklif ve ölçüm.

Ancak **ticari olarak “para kazanmaya hazır / tam otomatik” etiketi henüz verilemez**. Kalan iki zorunlu readiness kapısı:

1. Şahıs işletmesinin resmi `tacir` mi `esnaf` mı olduğunun sicil/oda üzerinden kesinleştirilmesi; tacirse gerçek 16 haneli MERSİS ve ticaret unvanı, esnafsa doğru yasal footer akışının seçilmesi.
2. İYS/KVKK operasyonunun gerçek işletme kimliğiyle tamamlanması ve B2B/B2C iletişim politikasının kayda alınması.

Bu ikisi tamamlanınca ilk adım `GMAIL_SEND_ENABLED=true` ile sınırsız otomasyon değildir. Önce 3–5 gerçek, yüksek uyumlu lead için supervised pilot yapılır; her ileti HITL onaylı kalır. Deliverability, reply, olumlu reply, görüşme ve teklif metriği gerçek provider outcome'larından doğrulanır.

## Bundan sonra tek sıra

1. Resmi tacir/esnaf statüsünü ve varsa MERSİS'i belgeyle kesinleştir.
2. İYS kaydını/ileti politikasını tamamla; Settings uyum alanlarına yalnız gerçek değerleri gir.
3. Production health 16/16 olunca ticari send flag'ini aç.
4. 3–5 lead supervised pilot çalıştır; kanıtsız iddia yok, her gönderim HITL.
5. İlk gerçek sonuçlardan sonra kalıcı A/B varyant attribution ve sektör/rol bazlı kazanan açı optimizasyonunu devreye al.

## Pazarlıksız güvenlik sabitleri

- Secret'lar terminale, git'e veya dokümana yazılmaz.
- Onaysız müşteri e-postası gönderilmez.
- `unknown` provider sonucu otomatik resend edilmez.
- Suppression/opt-out fail-closed kalır.
- App ve LIFE DB sınırları karıştırılmaz.
- Gerçek yasal kimlik yerine sahte/placeholder MERSİS yazılmaz.

## 2026-07-15 temiz başlangıç eki

- AgencyOS App DB müşteri/gelir operasyon verisi tam yedek + SHA-256 sonrasında temizlendi; 27/27 tablo yeniden sayımla `0` doğrulandı.
- LIFE alışkanlık/görev/rutin verisine dokunulmadı. Suppression, consent, settings, Gmail bağlantısı, hizmet kataloğu ve playbook'lar korundu.
- Yedek: `output/backups/agencyos-customer-data-2026-07-15_09-36-40-325.json`; SHA-256 `b9020d94202f043ff9e4f174cd92ca32b0dd28bea61e81a21dcecbd080890365`.
- Güvenli tekrar aracı: `scripts/clean-start.js`; exact App ref + exact confirmation, LIFE/E2E reddi, tam yedek, sıralı silme ve 27 tablo zero verification.
- Local yalnız-geliştirme env'i üretim callback/webhook URL'ini referanslıyor; Gmail ingest açık, ticari send kapalı.
- Local sunucu `http://localhost:3000/bugun` üzerinde bırakıldı. `127.0.0.1` yerine `localhost` kullanılmalı (Next dev-origin koruması).
- Son tarayıcı doğrulaması: `/bugun`, dua, `/harita`, `/firsatlar`, `/pipeline`, `/settings` PASS; Telegram `ready`, Gmail canary `replied`.
- Ayrıntı ve günlük kullanım: `docs/handoffs/2026-07-15-clean-start-final.md`.
